<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Wish Board - Share Your Dreams</title>
    <link rel="stylesheet" href="/styles/index2.css" />
    <script src="/scripts/index.js" defer></script>
  </head>
  <body>
    <div class="hearts-background"></div>

    <header class="welcome-header">
      <h1>Welcome to Our Love Story</h1>
      <p>Two hearts, one journey — dreaming, growing, and loving together every day.</p>
    </header>
    
    <section class="quote-carousel">
      <div class="quote-slide active">
        <blockquote>
          "In you, I’ve found my home, my heart, my everything." <cite>– Unknown</cite>
        </blockquote>
      </div>
      <div class="quote-slide">
        <blockquote>
          "Every love story is beautiful, but ours is my favorite."
          <cite>– Anonymous</cite>
        </blockquote>
      </div>
      <div class="quote-slide">
        <blockquote>
          "Together is a wonderful place to be."
          <cite>– Unknown</cite>
        </blockquote>
      </div>
    </section>
    
    <section class="wishes-showcase">
      <h2>Our Dreams Together</h2>
      <div class="wishes-grid">
        <div class="wish-card">
          <h3>Forever Us</h3>
          <p>I wish to hold her hand through every chapter of life — forever and always.</p>
          <span class="wish-author">– Mirjalol, 2025</span>
        </div>
        <div class="wish-card">
          <h3>Wander Together</h3>
          <p>We dream of exploring the world hand in hand, one breathtaking place at a time.</p>
          <span class="wish-author">– Fazilat, 2025</span>
        </div>
        <div class="wish-card">
          <h3>Endless Moments</h3>
          <p>My wish is to create a lifetime of little moments that mean everything.</p>
          <span class="wish-author">– Mirjalol, 2025</span>
        </div>
        <div class="wish-card">
          <h3>Love & Light</h3>
          <p>Together, I wish for a life filled with laughter, love, and endless support.</p>
          <span class="wish-author">– Fazilat, 2025</span>
        </div>
      </div>
    </section>
    
    <section class="cta-section">
      <div class="cta-container">
        <h2>Our love</h2>
        <p>Join us as we dream, grow, and build a future filled with love.</p>
        <div class="cta-buttons">
          <a href="/login" class="cta-button">Our Memories</a>
          <a href="/us" class="cta-button">About Us</a>
        </div>
      </div>
    </section>    
    <script>
document.addEventListener('DOMContentLoaded', () => {
  const heartsContainer = document.querySelector('.hearts-background');
  for (let i = 0; i < 15; i++) {
    const heart = document.createElement('div');
    heart.className = 'heart';
    heart.innerHTML = '❤️';
    heart.style.setProperty('--random-left', Math.random());
    heart.style.animationDuration = `${8 + Math.random() * 4}s`;
    heart.style.animationDelay = `${Math.random() * 4}s`;
    heart.style.fontSize = `${1 + Math.random() * 0.5}rem`;
    heartsContainer.appendChild(heart);
  }

  const slides = document.querySelectorAll('.quote-slide');
  let currentSlide = 0;

  function showSlide(index) {
    slides.forEach((slide, i) => {
      slide.classList.toggle('active', i === index);
    });
  }

  function nextSlide() {
    currentSlide = (currentSlide + 1) % slides.length;
    showSlide(currentSlide);
  }

  showSlide(currentSlide);
  setInterval(nextSlide, 5000);
});
    </script>
  </body>
</html>