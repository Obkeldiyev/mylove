<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Update <%= wish.title %></title>
    <link rel="stylesheet" href="/styles/wishes.update.css" />
  </head>
  <body>
    <!-- Navbar -->
    <nav class="navbar">
      <a href="/" class="nav-heart">❤️</a>
      <div class="nav-links">
        <a href="/notes" class="nav-button">Notes</a>
        <a href="/profile" class="nav-button">Profile</a>
        <a href="/wishes" class="nav-button">Wishes</a>
      </div>
    </nav>

    <!-- Form Header -->
    <header class="form-header">
      <h1>Update Wish</h1>
    </header>

    <!-- Form Container -->
    <div class="form-container">
      <form action="/wishes/<%= wish.id %>/update" method="POST">
        <input
          type="text"
          name="title"
          placeholder="Wish Title"
          value="<%= wish.title %>"
          required
        />
        <textarea
          name="description"
          placeholder="Wish Description"
          required
        ><%= wish.description %></textarea>
        <button type="submit">Update Wish</button>
      </form>
    </div>
  </body>
</html>